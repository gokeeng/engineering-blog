source "https://rubygems.org"

# Use GitHub Pages compatible gems for better compatibility
gem "github-pages", group: :jekyll_plugins

# Additional plugins
group :jekyll_plugins do
  gem "jekyll-sitemap"
  gem "jekyll-seo-tag"
end
